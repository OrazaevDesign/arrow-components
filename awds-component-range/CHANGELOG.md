# range — журнал версий

## 2.0.10 — 2026-09-25 — PATCH · visual
Фон стал плоской заливкой: из базы убран linear-gradient(chroma → bg), ячейки chroma-* компонентом больше не читаются, сняты регистрация @property и строка transition по ним. Градиент был виден в rest, hover и focus у заливки трека и у ручки.
Потребителю: Разметка и классы прежние. Фон контрола теперь ровный — заметно там, где градиент был виден (в rest, hover и focus у заливки трека и у ручки). Потребителям с копией CSS нужна пересборка секции.

## 2.0.9 — 2026-09-15 — PATCH · visual
Якоря #cell обёрнуты в комментарий /* */. Голый якорь, введённый 15.09.2026, комментарием не является: парсер CSS считает его битым объявлением и отбрасывает вместе со всем до следующей точки с запятой, то есть съедает следующую строку. Восстановлены фон и тень ручки: 30 элементов превью из 143.
Потребителю: Пересобрать копию CSS: блокам уедет исправленная секция.

## 2.0.8 — 2026-09-15 — PATCH
Проставлены якоря #cell: каждая строка, читающая роль, называет ячейку макета, из которой роль взята. Меняются только комментарии — значения, классы и контракт те же. Нужно для гейта component-cell-drift.mjs, который сверяет цель ячейки с ролью в коде.
Потребителю: Ничего делать не нужно: изменились только комментарии в CSS.

## 2.0.7 — 2026-09-14 — PATCH · visual
Ячейки State и Size заменены целями: роли и шкала space вместо --awds-state-range-* и --awds-size-square-N-handle. Коллекции приватные с 03.09.2026, этих имён в теме нет — компонент рендерился никак: трек и заливка прозрачные, ручка 0×0, высота 4px вместо 20/24. Семь целей из семи совпали по hex с ячейками макета.
Потребителю: Ничего делать не нужно: классы, разметка и значения те же. Компонент, наоборот, начинает отображаться — если в блоке слайдер выглядел пустой полосой, после обновления секции появятся трек, заливка и ручка.

## 2.0.6 — 2026-09-14 — PATCH
Переведён на новый файл Figma «💠 arrow ↪ components», секция ↪ range (5:35): снапшот, шапки range.css/range.js, ссылки в range-default.md и range-light.md. Структура макета сменилась с одного набора (12 ячеек) на два по варианту (8 ячеек), ступень 300 из макета ушла — код уже был без неё. Сырых заливок ноль, токены совпали с метой один в один; CSS не менялся.
Потребителю: Ничего делать не нужно: классы, переменные и значения те же, изменились только ссылки на макет.

## 2.0.5 — 2026-09-02 — PATCH
Инвариантные ячейки слоя State схлопнуты в одну без суффикса состояния (решение владельца 02.09.2026, план 2026-09-02 - state-vocab-decisions.md): компонент читает --awds-state-…-bg вместо четвёрки -rest/-hover/-focus/-active с одинаковыми значениями. Значения не менялись.
Потребителю: Разметка и классы прежние; блокам придёт обновлённая секция. Публикация блоков и темы — после Э3.

## 2.0.4 — 2026-09-02 — PATCH
Имена переменных темы переведены на новые после переименования семейств в студии 02.09.2026 (space, opacity, typography, wysiwyg, layout, breakpoints, zindex без удвоенного сегмента). Значения за именами те же — сверено по 733 парам, визуально ничего не меняется.
Потребителю: Разметка и классы прежние, править нечего; блокам придёт обновлённая секция CSS. Публикация блоков — только вместе с публикацией темы: старая тема на проде новых имён не знает.

## 2.0.3 — 2026-09-01 — PATCH
Ссылки на слой State переведены с mode-less имён на плоские суффиксные: opacity/control-disabled → те же ячейки с суффиксом -rest. Значение не изменилось: у этих ячеек все четыре состояния (rest, hover, focus, active) несут одно и то же, поэтому -rest подставлен без потери смысла. Смысл правки — снять зависимость компонента от блоков-обёрток темы, которые переключают mode-less имена за него и новых компонентов уже не покрывают. Этап Э2 плана «Уплощение слоя State».
Потребителю: Ничего делать не надо: имена читаются внутри CSS компонента, разметка, классы и вид не менялись.

## 2.0.2 — 2026-09-01 — PATCH
Гашение переведено на ячейку слоя State: opacity: var(--awds-state-opacity-control-disabled) вместо литерала --awds-opacity-opacity-40 (решение Р9 — значение то же, 40%). Заодно снята документационная отставшая ступень 300: SKILL.md и мета (preview.matrix_sizes, ds_token_dependencies) всё ещё перечисляли 500·400·300, хотя из CSS и контракта ступень ушла ещё в 2.0.0 от 28.08. Из ds_token_dependencies убран --awds-size-square-300-handle.
Потребителю: Пересобрать копию CSS (sync-component-css). Вид не меняется: --awds-state-opacity-control-disabled резолвится в те же 40%. Разметку править не нужно — контракт тот же. Если где-то ещё стоит .range--300, ступени нет с 2.0.0: брать .range--400 (диаметр ручки тот же, 20px).

## 2.0.1 — 2026-08-30 — PATCH
Описание скилла сокращено до имени и базового класса: маршрут к компоненту переехал в awds-components.
Потребителю: Ничего делать не нужно — разметка, CSS и токены не менялись.

## 2.0.0 — 2026-08-28 — MAJOR
Ступень 300 убрана: она ссылалась на свой токен square-300-handle, но в теме он равен square-400-handle, и в макете обе рисовались 20px. Ступень, неотличимая от соседней, — выбор без последствий: потребитель гадает, какую взять, а разницы нет. Понадобится размер мельче 400 — заведём с собственным значением. Шкала стала 400 и 500. В Figma компонент разложен на фреймы range / default и range / light; ползунок — составная часть, фреймы . / handle / default и . / handle / light (точка = в контракт не попадает).
Потребителю: В разметке: range--300 → range--400 (вид не изменится, оба дают 20px). Потребителей в блоках нет.

## 1.0.4 — 2026-08-28 — PATCH
Слой фокуса переименовал переменные тона: --awds-focus-color-formcontrol → -muted, --awds-focus-color-tab → -accent (focus-selection 2.0.0). Ссылки в CSS компонента обновлены.
Потребителю: Пересобрать копию CSS: значения не менялись, поменялись имена переменных.

## 1.0.3 — 2026-08-27 — PATCH · visual
Ползунок Light получил тень elevation-1 из макета; кольцо фокуса переведено на вариант Formcontrol слоя и горит только на той ручке, чей input в фокусе, и только по :focus-visible; выключение теперь читается и с атрибута disabled на инпутах
Потребителю: Пересобрать копию CSS. Для выключенного слайдера ставить disabled на каждом input: класс .range--disabled только красит, с клавиатуры значение менялось

## 1.0.2 — 2026-08-26 — PATCH
Цвета состояний переведены с инлайна ролей на ячейки слоя State темы студии.
Потребителю: Ничего: конечные значения совпали до одного (проверено резолвом 858 замен). Пересобрать копию CSS в блоках.

## 1.0.1 — 2026-08-25 — PATCH
Превью ссылается на derived.css: слой производных переименован из studio-compat.css (в нём остался только композит тени).
Потребителю: Ничего: правка только в preview.html, CSS компонента не менялся.

## 1.0.0 — 2026-08-24 — INIT · visual
Точка отсчёта версий. В состав входит переезд кольца фокуса в слой awds-component-focus-selection (21.08): у потребителей стоит прежняя геометрия, поэтому релиз помечен visual.
Потребителю: пересобрать копии CSS в блоках и посмотреть превью — зазор кольца фокуса изменился.
