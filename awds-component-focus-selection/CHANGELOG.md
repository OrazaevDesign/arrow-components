# focus-selection — журнал версий

## 2.0.9 — 2026-09-15 — PATCH · visual
Якоря #cell обёрнуты в комментарий /* */. Голый якорь, введённый 15.09.2026, комментарием не является: парсер CSS считает его битым объявлением и отбрасывает вместе со всем до следующей точки с запятой, то есть съедает следующую строку. Восстановлены цвет и ширина кольца: 31 элемент превью из 117.
Потребителю: Пересобрать копию CSS: блокам уедет исправленная секция.

## 2.0.8 — 2026-09-15 — PATCH
Проставлены якоря #cell: каждая строка, читающая роль, называет ячейку макета, из которой роль взята. Меняются только комментарии — значения, классы и контракт те же. Нужно для гейта component-cell-drift.mjs, который сверяет цель ячейки с ролью в коде.
Потребителю: Ничего делать не нужно: изменились только комментарии в CSS.

## 2.0.7 — 2026-09-14 — PATCH
Код переведён с ячеек State/Size на их цели: кольцо читает роли surface-on-highest и primary-core и шаги space-0-5 / space-px вместо --awds-state-focus-selection-* и --awds-size-outline-*. Макет перечитан из нового файла 💠 arrow ↪ components (секция 5:27, набор 6:36): оси переименованы в radius × offset × tone, обводка во всех 54 ячейках привязана к size/outline/width. Ячейки focus-selection/outlineShow-* удалены из студии как мёртвые — их не читал ни макет, ни код.
Потребителю: Ничего делать не нужно: имена и значения переменных контракта --awds-focus-* не менялись, вид не изменился.

## 2.0.6 — 2026-09-02 — PATCH
Инвариантные ячейки слоя State схлопнуты в одну без суффикса состояния (решение владельца 02.09.2026, план 2026-09-02 - state-vocab-decisions.md): компонент читает --awds-state-…-bg вместо четвёрки -rest/-hover/-focus/-active с одинаковыми значениями. Значения не менялись.
Потребителю: Разметка и классы прежние; блокам придёт обновлённая секция. Публикация блоков и темы — после Э3.

## 2.0.5 — 2026-09-02 — PATCH
Имена переменных темы переведены на новые после переименования семейств в студии 02.09.2026 (space, opacity, typography, wysiwyg, layout, breakpoints, zindex без удвоенного сегмента). Значения за именами те же — сверено по 733 парам, визуально ничего не меняется.
Потребителю: Разметка и классы прежние, править нечего; блокам придёт обновлённая секция CSS. Публикация блоков — только вместе с публикацией темы: старая тема на проде новых имён не знает.

## 2.0.4 — 2026-09-01 — PATCH
Ссылки на слой State переведены с mode-less имён на плоские суффиксные: focus-selection/outline, focus-selection/outlinevariant → те же ячейки с суффиксом -rest. Значение не изменилось: у этих ячеек все четыре состояния (rest, hover, focus, active) несут одно и то же, поэтому -rest подставлен без потери смысла. Смысл правки — снять зависимость компонента от блоков-обёрток темы, которые переключают mode-less имена за него и новых компонентов уже не покрывают. Этап Э2 плана «Уплощение слоя State».
Потребителю: Ничего делать не надо: имена читаются внутри CSS компонента, разметка, классы и вид не менялись.

## 2.0.3 — 2026-08-30 — PATCH
Описание скилла сокращено до имени и базового класса: маршрут к компоненту переехал в awds-components.
Потребителю: Ничего делать не нужно — разметка, CSS и токены не менялись.

## 2.0.2 — 2026-08-29 — PATCH
Продолжение 2.0.1, которое оказалось неполным: прежний словарь Figma (Formcontrol, Tab, Type=, Var=, Size=) оставался ещё в прозе SKILL, в рецептах focus-selection-default.md, в подписях и проверках превью и в поле axes меты. Всё приведено к именам осей макета: offset (inside/outside), tone (default/muted/accent), radius. Правила CSS не менялись ни в 2.0.1, ни здесь.
Потребителю: Ничего: только документация, превью и комментарии. Пересобрать пин — sync-component-css.mjs, затем component-usage.mjs --upgrade focus-selection.

## 2.0.1 — 2026-08-29 — PATCH
Доделано переименование 2.0.0: в комментарии CSS, таблице осей SKILL, зеркальных классах превью и ключах меты оставались прежние formcontrol и tab. Заодно оси названы так же, как в макете — offset / tone / radius вместо Type / Var / Size.
Потребителю: Ничего: правила CSS не менялись, только комментарий, документация и превью. Нужен пересбор пина — sync-component-css.mjs, затем component-usage.mjs --upgrade focus-selection.

## 2.0.0 — 2026-08-28 — MAJOR
Тон вынесен в свою ось и назван по тому, что меняется, а не по потребителю: классы focus-selection-muted / -accent вместо --formcontrol / --tab, переменные --awds-focus-color-muted / -accent. В Figma компонент схлопнут в один фрейм с осями radius × offset × tone.
Потребителю: В разметке: .focus-selection--formcontrol → .focus-selection-muted, .focus-selection--tab → .focus-selection-accent. В CSS: var(--awds-focus-color-formcontrol) → -muted, var(--awds-focus-color-tab) → -accent. Цвета не менялись. Пересобрать секции (sync-component-css) и перепубликовать блоки.

## 1.1.1 — 2026-08-28 — PATCH
Комментарии CSS приведены к именам осей в макете: type → offset, size → radius; варианты стали отдельными фреймами control · form-control · tab, ссылка ведёт на страницу компонента.
Потребителю: Содержимое копии не меняется — комментарии файла в секцию блока не копируются, у неё свой заголовок. Нужен только пересбор пина: sync-component-css.mjs, затем component-usage.mjs --upgrade.

## 1.1.0 — 2026-08-26 — MINOR
Слой сам надевает кольцо: правило :where(.btn):focus-visible переехало из компонентов в слой. Список потребителей ведётся здесь и пополняется по мере перевода.
Потребителю: Пересобрать секции: правило кольца теперь приезжает вместе с контрактом слоя, а не из CSS компонента. Кольцо в блоках без пересборки пропадёт.

## 1.0.3 — 2026-08-25 — PATCH
Превью ссылается на derived.css: слой производных переименован из studio-compat.css (в нём остался только композит тени).
Потребителю: Ничего: правка только в preview.html, CSS компонента не менялся.

## 1.0.2 — 2026-08-25 — PATCH
Толщина и зазор кольца берутся из ячеек студии Size/outline/{width,offset} вместо выбранной нами ступени шкалы Space.
Потребителю: Ничего: computed не изменился (2px / 1px). Зазор кольца теперь правится дизайнером в студии, без релиза компонента.

## 1.0.1 — 2026-08-24 — PATCH
Слой перестал держать свои числа: толщина и зазор кольца берутся из шкалы темы (space-0-5, space-px), цвета — из коллекции State (focus-selection/outline и /outlinevariant) вместо ролей напрямую.
Потребителю: ничего: computed-значения не изменились (2px / 1px / -2px, rgb(20 20 20), rgba(250 223 66 / .5)). Кольцо теперь правится в студии.

## 1.0.0 — 2026-08-24 — INIT
Точка отсчёта версий. Артефакты зафиксированы как есть, вид не менялся.
Потребителю: ничего.
