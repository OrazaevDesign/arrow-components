# emblem — журнал версий

> До версии 3.0.0 компонент назывался `avatar` (базовый класс `.avatar`). Записи ниже 3.0.0 говорят о нём.

## 3.0.6 — 2026-09-25 — PATCH · visual
Ячейка emblem/color подтянута за студией: primary-container-on → primary-container-on-high.
Потребителю: Разметка и классы прежние. Инициалы и силуэт стали контрастнее. Потребителям с копией CSS нужна пересборка секции.

## 3.0.5 — 2026-09-15 — PATCH · visual
Переведён на роли: единственная ссылка на ячейку --awds-state-opacity-content-placeholder заменена её целью var(--awds-opacity-60). Имени ячейки в опубликованной теме нет с 03.09.2026 — объявление молча отбрасывалось, и силуэт-заглушка рисовался в полную силу вместо 60%. Проставлены семь якорей #cell (сразу в комментариях). У вида icon кольцо снимается правилом box-shadow: none вместо прозрачного цвета в аккумуляторе: в макете обводки у него нет вовсе, а прозрачный цвет требовал бы ячейки, которой в словаре нет.
Потребителю: Пересобрать копию CSS. Силуэт-заглушка теперь приглушён до 60%, как в макете; остальное без изменений.

## 3.0.4 — 2026-09-14 — PATCH
Переведён на новый файл Figma «💠 arrow ↪ components», секция ↪ emblem (5:40): снапшот, шапка emblem.css, ссылки в четырёх reference-файлах. Четыре вида, лежавшие отдельными фреймами на странице 0:1 старого файла, собраны в наборы emblem / initials · img · placeholder · icon внутри одной секции; состав ступеней не менялся. Диаметры, типографика и цвета сошлись без правок. Сырых заливок у компонента ноль — найденные 15 лежат внутри дефолтной иконки слота (цветной флаг ОАЭ) и цветами роли быть не должны.
Потребителю: Ничего делать не нужно: классы, переменные и значения те же.

## 3.0.3 — 2026-09-02 — PATCH
Инвариантные ячейки слоя State схлопнуты в одну без суффикса состояния (решение владельца 02.09.2026, план 2026-09-02 - state-vocab-decisions.md): компонент читает --awds-state-…-bg вместо четвёрки -rest/-hover/-focus/-active с одинаковыми значениями. Значения не менялись.
Потребителю: Разметка и классы прежние; блокам придёт обновлённая секция. Публикация блоков и темы — после Э3.

## 3.0.2 — 2026-09-02 — PATCH
Имена переменных темы переведены на новые после переименования семейств в студии 02.09.2026 (space, opacity, typography, wysiwyg, layout, breakpoints, zindex без удвоенного сегмента). Значения за именами те же — сверено по 733 парам, визуально ничего не меняется.
Потребителю: Разметка и классы прежние, править нечего; блокам придёт обновлённая секция CSS. Публикация блоков — только вместе с публикацией темы: старая тема на проде новых имён не знает.

## 3.0.1 — 2026-09-01 — PATCH
Ссылки на слой State переведены с mode-less имён на плоские суффиксные: opacity/content-placeholder → те же ячейки с суффиксом -rest. Значение не изменилось: у этих ячеек все четыре состояния (rest, hover, focus, active) несут одно и то же, поэтому -rest подставлен без потери смысла. Смысл правки — снять зависимость компонента от блоков-обёрток темы, которые переключают mode-less имена за него и новых компонентов уже не покрывают. Этап Э2 плана «Уплощение слоя State».
Потребителю: Ничего делать не надо: имена читаются внутри CSS компонента, разметка, классы и вид не менялись.

## 3.0.0 — 2026-09-01 — MAJOR · visual
Компонент переименован из avatar в emblem: базовый класс .avatar → .emb, все классы видов и ступеней вместе с ним, файлы references/avatar*.{css,md} → emblem*.{css,md}. Добавлен четвёртый вид .emb-icon — произвольная иконка на подложке surface-bright, без кольца, ступени 150 · 100 · 50. Ступень 700 приведена к макету: 80px (space-20) вместо 72px. Скругление переведено с хардкода 50% на ячейку var(--awds-rounded-border-radius-full), гашение силуэта — на var(--awds-state-opacity-content-placeholder).
Потребителю: Заменить .avatar → .emb, .avatar-{initials,img,placeholder} → .emb-{…}, .avatar--{N} → .emb--{N}; подключаемый файл теперь references/emblem.css. Вид изменился: круг ступени 700 стал 80px вместо 72px — проверить раскладку там, где он стоит. На 01.09.2026 потребитель один — awds-component-button-profile, и он переделывается в button-menu отдельным шагом; в блоках PageCraft класса .avatar нет.

## 2.0.1 — 2026-08-30 — PATCH
Описание скилла сокращено до имени и базового класса: маршрут к компоненту переехал в awds-components.
Потребителю: Ничего делать не нужно — разметка, CSS и токены не менялись.

## 2.0.0 — 2026-08-28 — MAJOR
Вариант picture переименован в img. В Figma компонент разложен на отдельные фреймы avatar / {вариант}, оси варианта больше нет — имя фрейма стало именем класса.
Потребителю: Заменить класс .avatar-picture на .avatar-img. Вид не менялся. В блоках компонент не используется — рассылка копий CSS не нужна.

## 1.0.2 — 2026-08-26 — PATCH
Превью ссылается на derived.css: слой производных переименован из studio-compat.css.
Потребителю: Ничего: правка только в preview.html, CSS компонента не менялся.

## 1.0.1 — 2026-08-25 — PATCH
Превью ссылается на derived.css: слой производных переименован из studio-compat.css (в нём остался только композит тени).
Потребителю: Ничего: правка только в preview.html, CSS компонента не менялся.

## 1.0.0 — 2026-08-24 — INIT
Точка отсчёта версий. Артефакты зафиксированы как есть, вид не менялся.
Потребителю: ничего.
