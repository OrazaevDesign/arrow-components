# button-overhung — журнал версий

## 2.0.14 — 2026-09-26 — PATCH
Толщина обводки перестала быть литералом: 1px заменён на var(--awds-space-px) — цель ячейки size/outline/offset. Литерал сидел внутри двухслойного box-shadow на строке-продолжении, и гейт own_values его не видел: сканер требовал «свойство:» в той же строке. Гейт в тот же день переведён на разбор по объявлениям.
Потребителю: Пересобрать копию CSS: вид не меняется, 1px остаётся 1px. Разметка, классы и контракт прежние.

## 2.0.13 — 2026-09-25 — PATCH · visual
Цвет текста во всех состояниях кроме hover: ячейка overhung/*/color-{rest,focus,active} переведена в студии с roles.surface.on на roles.surface.on-high
Потребителю: Текст кнопки стал заметно контрастнее: было 2.6:1 на светлой теме и 2.98:1 на тёмной — ниже порога 3:1, стало 5.18:1 и 9.33:1

## 2.0.12 — 2026-09-15 — PATCH · visual
Код переведён с приватных ячеек State/Size на роли и базовые шкалы — 98 мёртвых ссылок в двух файлах, 102 якоря #cell. До починки кнопка рисовалась 47×20 px вместо 102×52: без отступов, без таблетки, без тени elevation и не своим цветом — 125 элементов превью из 255. В макете ось loading сведена в state (140 → 84 ячейки на набор), 28 обводок кольца загрузки перепривязаны со старой библиотеки.
Потребителю: Классы и разметка не менялись: контракт тот же. Кнопка начинает выглядеть так, как задумана — если блок подгонял её вручную, подгонку надо снять.

## 2.0.11 — 2026-09-14 — PATCH
Разметка в сниппетах и комментарии переведены на .progress--indeterminate: progress 2.0.0 убрал модификатор с класса варианта (правило 11).
Потребителю: Ничего делать не нужно, если разметка берётся из сниппетов скилла. Свой скопированный ранее код с progress-circular--indeterminate поправить: класс больше не объявлен.

## 2.0.10 — 2026-09-02 — PATCH
Инвариантные ячейки слоя State схлопнуты в одну без суффикса состояния (решение владельца 02.09.2026, план 2026-09-02 - state-vocab-decisions.md): компонент читает --awds-state-…-bg вместо четвёрки -rest/-hover/-focus/-active с одинаковыми значениями. Значения не менялись.
Потребителю: Разметка и классы прежние; блокам придёт обновлённая секция. Публикация блоков и темы — после Э3.

## 2.0.9 — 2026-09-02 — PATCH
Имена переменных темы переведены на новые после переименования семейств в студии 02.09.2026 (space, opacity, typography, wysiwyg, layout, breakpoints, zindex без удвоенного сегмента). Значения за именами те же — сверено по 733 парам, визуально ничего не меняется.
Потребителю: Разметка и классы прежние, править нечего; блокам придёт обновлённая секция CSS. Публикация блоков — только вместе с публикацией темы: старая тема на проде новых имён не знает.

## 2.0.8 — 2026-09-01 — PATCH
Ссылки на слой State переведены с mode-less имён на плоские суффиксные: opacity/control-disabled → те же ячейки с суффиксом -rest. Значение не изменилось: у этих ячеек все четыре состояния (rest, hover, focus, active) несут одно и то же, поэтому -rest подставлен без потери смысла. Смысл правки — снять зависимость компонента от блоков-обёрток темы, которые переключают mode-less имена за него и новых компонентов уже не покрывают. Этап Э2 плана «Уплощение слоя State».
Потребителю: Ничего делать не надо: имена читаются внутри CSS компонента, разметка, классы и вид не менялись.

## 2.0.7 — 2026-09-01 — PATCH
Гашение выключенного состояния переведено с литерала --awds-opacity-opacity-40 на ячейку слоя State --awds-state-opacity-control-disabled. Значение за ячейкой то же, что было за литералом, — 40 %. Скилл и рецепты приведены к тому же имени ячейки.
Потребителю: Ничего делать не надо: вид не изменился. Если у вас лежит своя копия CSS — пересоберите её, чтобы гашение поехало из темы, а не из литерала.

## 2.0.6 — 2026-08-31 — PATCH
Ось флага загрузки в макете названа loading=off|on — одно слово на семью кнопок и то же, что модификатор в коде. Оси, padding и размер кольца загрузки полностью совпадают с макетом; расхождений не осталось.
Потребителю: Ничего делать не нужно: CSS и разметка не менялись.

## 2.0.5 — 2026-08-31 — PATCH
Сверка с исправленным макетом: оси content=/state=hover|focus/progress=off|on, горизонтальный padding и размер кольца загрузки перевязаны на rectangle/{N}/padding и rectangle/{N}/icon — код совпадает с макетом на всех семи ступенях без правок. Документация и снапшот приведены к этому состоянию.
Потребителю: Ничего делать не нужно: CSS и разметка не менялись.

## 2.0.4 — 2026-08-31 — PATCH
Оси макета приведены к канону (content=, state=hover/focus, progress=off|on) — документация и снапшот сверены по 140 ячейкам обоих вариантов. Ширины ячеек 600 и 500 в макете стали 156 и 144 и совпали с кодом.
Потребителю: Ничего делать не нужно: CSS и разметка не менялись.

## 2.0.3 — 2026-08-31 — PATCH
Документация под правку макета от 31.08.2026: ось type= переименована в content=, горизонтальный padding перевязан с button-input на rectangle/{N}/padding — расхождения, описанные при 2.0.2, закрыты. Снапшот Figma перечитан по привязкам узлов.
Потребителю: Ничего делать не нужно: CSS и разметка не менялись — правка только в документации и снапшоте.

## 2.0.2 — 2026-08-31 — PATCH · visual
Радиус — таблетка на всех ступенях: border-radius в базовом .obtn ссылается на --awds-rounded-border-radius-full, из размерных блоков убран (в макете все семь ступеней и icon-only привязаны к border-radius/full). Документация и preview приведены к тому, что реально в CSS: ячейки слоя State вместо rgb(var(--surface-*)), --awds-size-rectangle-* вместо --awds-rectangle-*.
Потребителю: Обновить копию CSS в блоке: кнопка станет таблеткой на всех размерах, icon-only — кругом. Разметка и классы не меняются. Если блок опирался на ступенчатое скругление overhung — проверить композицию рядом стоящих элементов.

## 2.0.1 — 2026-08-30 — PATCH
Описание скилла сокращено до имени и базового класса: маршрут к компоненту переехал в awds-components.
Потребителю: Ничего делать не нужно — разметка, CSS и токены не менялись.

## 2.0.0 — 2026-08-29 — MAJOR · visual
То же, что у button: .obtn__progress стал слотом под кольцо awds-component-progress, свои keyframes obtn-spin убраны
Потребителю: Править разметку загрузки так же, как у button; блоку нужна секция progress

## 1.0.5 — 2026-08-26 — PATCH
Цвета состояний переведены с инлайна ролей на ячейки слоя State темы студии.
Потребителю: Ничего: конечные значения совпали до одного (проверено резолвом 858 замен). Пересобрать копию CSS в блоках.

## 1.0.4 — 2026-08-25 — PATCH
Ссылки на справочники ADS переведены на tokens-map.md: шесть справочников со значениями удалены, значения живут в studio-vars.css.
Потребителю: Ничего: правка только в документации компонента, CSS не менялся.

## 1.0.3 — 2026-08-25 — PATCH
Превью ссылается на derived.css: слой производных переименован из studio-compat.css (в нём остался только композит тени).
Потребителю: Ничего: правка только в preview.html, CSS компонента не менялся.

## 1.0.2 — 2026-08-24 — PATCH
Иконка внутри кнопки объявлена блочной (.obtn svg { display: block }) — правило жило только в копиях блоков.
Потребителю: пересобрать копию CSS. Вид не меняется: в flex-раскладке svg уже блочный, правило страхует от baseline-зазора при переопределении раскладки блоком.

## 1.0.1 — 2026-08-24 — PATCH
В компонент подняты две вещи, которые жили только в копиях внутри блоков: подавление вспышки тапа (-webkit-tap-highlight-color) и базовое правило слота .obtn__content (центрирование содержимого).
Потребителю: пересобрать копию CSS (sync-component-css). Вид в покое не меняется; на мобильных пропадает серая вспышка при тапе, у кнопок с текстом содержимое центрируется слотом, а не наследованным выравниванием.

## 1.0.0 — 2026-08-24 — INIT · visual
Точка отсчёта версий. В состав входит переезд кольца фокуса в слой awds-component-focus-selection (21.08): у потребителей стоит прежняя геометрия, поэтому релиз помечен visual.
Потребителю: пересобрать копии CSS в блоках и посмотреть превью — зазор кольца фокуса изменился.
