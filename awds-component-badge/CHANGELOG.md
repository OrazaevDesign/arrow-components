# badge — журнал версий

## 1.1.5 — 2026-09-26 — PATCH
Толщина обводки перестала быть литералом: 1px заменён на var(--awds-space-px) — цель ячейки size/outline/offset. Значение то же (1px), меняется источник: толщина правится в студии, а не в файле компонента. Обоснование «ячейки толщины в студии нет» в own_values протухло — ячейка заведена, запись снята.
Потребителю: Пересобрать копию CSS: вид не меняется, 1px остаётся 1px. Разметка, классы и контракт прежние.

## 1.1.4 — 2026-09-15 — PATCH · visual
Якоря #cell обёрнуты в комментарий /* */. Голый якорь, введённый 15.09.2026, комментарием не является: парсер CSS считает его битым объявлением и отбрасывает вместе со всем до следующей точки с запятой, то есть съедает следующую строку. Восстановлены chroma, border и color почти во всех вариантах: 195 элементов превью из 279 рисовались неправильно.
Потребителю: Пересобрать копию CSS: блокам уедет исправленная секция.

## 1.1.3 — 2026-09-15 — PATCH
Проставлены якоря #cell: каждая строка, читающая роль, называет ячейку макета, из которой роль взята. Меняются только комментарии — значения, классы и контракт те же. Нужно для гейта component-cell-drift.mjs, который сверяет цель ячейки с ролью в коде.
Потребителю: Ничего делать не нужно: изменились только комментарии в CSS.

## 1.1.2 — 2026-09-14 — PATCH · visual
Код переведён с ячеек State/Size на их цели: 68 ссылок --awds-state-badge-* стали ролями семейств (info/success/warning/error/…), 35 ссылок --awds-size-rectangle-* — шагами space, rounded и control. Макет перечитан из нового файла 💠 arrow ↪ components (секция 5:29): 17 наборов badge / {семья}-{вариант}, оси content × size, заглушки Type/Var из имён ушли. В макете выровнена ступень size=200 — 34 ячейки читали rectangle/100/rounded вместо своей.
Потребителю: ВИД МЕНЯЕТСЯ, и это починка. До правки бейдж ссылался на имена, которых в опубликованной теме нет (коллекции State и Size приватные с 03.09.2026): иконка выходила нулевого размера, отступов и радиуса не было, цвет наследовался от родителя. Замер превью: 60 элементов, 268 свойств. Теперь бейдж рисуется так, как задуман. Перепроверить блоки, где бейдж стоял и «случайно вписывался» в неверном виде.

## 1.1.1 — 2026-08-30 — PATCH
Описание скилла сокращено до имени и базового класса: маршрут к компоненту переехал в awds-components.
Потребителю: Ничего делать не нужно — разметка, CSS и токены не менялись.

## 1.1.0 — 2026-08-28 — MINOR
Добавлены семейства System (7) и Status (6) — всего 17 вариантов на прежней геометрии. Цвет каждого берётся ячейками слоя State темы; своих значений не прибавилось. Карточки market-вариантов приведены к ячейкам State вместо ролей.
Потребителю: Ничего: прежние четыре варианта и разметка не менялись. Обновить копию CSS, чтобы стали доступны badge-system-* и badge-status-*.

## 1.0.2 — 2026-08-26 — PATCH
Цвета состояний переведены с инлайна ролей на ячейки слоя State темы студии.
Потребителю: Ничего: конечные значения совпали до одного (проверено резолвом 858 замен). Пересобрать копию CSS в блоках.

## 1.0.1 — 2026-08-25 — PATCH
Превью ссылается на derived.css: слой производных переименован из studio-compat.css (в нём остался только композит тени).
Потребителю: Ничего: правка только в preview.html, CSS компонента не менялся.

## 1.0.0 — 2026-08-24 — INIT
Точка отсчёта версий. Артефакты зафиксированы как есть, вид не менялся.
Потребителю: ничего.
