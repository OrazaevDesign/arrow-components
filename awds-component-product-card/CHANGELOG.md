# product-card — журнал версий

## 1.1.11 — 2026-09-23 — PATCH
Ссылки на переименованные токены лейаута: --awds-layout-card-slider-min|max → --awds-layout-card-product-min|max. Значения прежние (128 → 170 и 256 → 368), вид не изменился.
Потребителю: После публикации темы обновить копию CSS в блоках через sync-component-css.mjs: переменных со старыми именами в теме больше нет.

## 1.1.10 — 2026-09-19 — PATCH
Часть .blog в макете лишилась оси view из одного значения — стала одиночным компонентом; словарь частей уточнён по фактическому составу (дата, скрытый счётчик комментариев, просмотры, три текстовых свойства).
Потребителю: Ничего делать не нужно: часть в коде не реализована, CSS не менялся.

## 1.1.9 — 2026-09-19 — PATCH
Комментарий у звезды рейтинга больше не несёт устаревший hex: цвет описан ячейкой rating/selected/bg-rest → warning-core. Правок значений нет — макет переведён на новую библиотеку токенов, код уже держал эти роли.
Потребителю: Ничего делать не нужно: роли и числа не менялись.

## 1.1.8 — 2026-09-18 — PATCH · visual
Числовая сверка кода с макетом: флаг страны приведён к ячейкам rectangle/{400,200}/icon и /padding (24px в обеих вью → 20 и 16, бокс как был 40 и 32), в brand-first снято мобильное мельчание бренда, превью и комментарии переведены с .typo-small на medium. Страница 9 · commerce вычищена от старых библиотек по всем трём слоям: шесть инстансов Slider свапнуты на локальный (−122 привязки), 67 явных модов старых коллекций сняты с переносом трёх на новые, четыре стиля button/clear/* переведены на extended/transparent, а в .tips четыре части . / text возвращены со старой ◆/Text/Semibold, подменённой swap-оверрайдом (−80). Своих привязок и модов к старым библиотекам не осталось; остаток 105 приходит от мастера input.
Потребителю: Флаг страны стал меньше и точнее по макету (20px на десктопе, 16px на мобиле вместо 24px везде), бокс вокруг него не изменился. В brand-first бренд на мобильной ширине больше не мельчает. Разметка и классы прежние; пересобрать копии CSS в блоках.

## 1.1.7 — 2026-09-18 — PATCH · visual
Компонент переведён с приватных ячеек State/Size на роли и базовые шкалы — последний из 41. 31 ссылка заменена значением цели, 42 якоря #cell. Макет переехал в новый файл 💠 arrow ↪ components: секция ↪ product-card (465:51861) и словарь частей ↪ part (5:53, 15 наборов); 903 привязки переписаны на 📜 tokens ↪ studio, трое устаревших внешних мастеров свапнуты на локальные. Литерал min-width 170px заменён на токен --awds-layout-card-slider-min: ячейка в студии есть, а own_values утверждал обратное, и хардкод терял адаптив (128px на мобиле). Сплошная проверка канона по требованию владельца привела к канону имена вариантов и осей (включая единое view=desktop-tablet|mobile во всех наборах и offer вместо kind у .price), имена 24 слоёв и порядок значений четырёх осей.
Потребителю: Вид изменился в одном месте: звезда рейтинга была чёрной и стала оранжевой (warning-core) — ячейка rating/selected/bg в опубликованной теме не существует с 03.09, и компонент просто не получал цвет. На мобильной ширине карточка снова сжимается до 128px вместо 170px, как задумано токеном раскладки. Разметка и классы прежние; пересобрать копии CSS в блоках.

## 1.1.6 — 2026-09-15 — PATCH
Кнопка избранного переведена с удалённого .btn-favorites--mobile на ступень шкалы .btn--200 (button-favorites 2.0.0).

## 1.1.5 — 2026-09-14 — PATCH
Комментарии и описания в CSS приведены к новому имени ячейки темы: form-control/*/placehold и list/*/placehold стали placeholder после исправления опечатки по всей цепочке 14.09.2026. Значения и селекторы не менялись.
Потребителю: Ничего делать не нужно.

## 1.1.4 — 2026-09-02 — PATCH
Инвариантные ячейки слоя State схлопнуты в одну без суффикса состояния (решение владельца 02.09.2026, план 2026-09-02 - state-vocab-decisions.md): компонент читает --awds-state-…-bg вместо четвёрки -rest/-hover/-focus/-active с одинаковыми значениями. Значения не менялись.
Потребителю: Разметка и классы прежние; блокам придёт обновлённая секция. Публикация блоков и темы — после Э3.

## 1.1.3 — 2026-09-02 — PATCH
Комментарии в CSS и SKILL.md приведены к новым именам ролей WYSIWYG (--awds-wysiwyg-*-lead/body/caption без удвоенного сегмента) — вслед за переименованием семейств в студии 02.09.2026. Правила и значения не менялись.
Потребителю: Ничего: изменились только комментарии.

## 1.1.2 — 2026-09-02 — PATCH
Имена переменных темы переведены на новые после переименования семейств в студии 02.09.2026 (space, opacity, typography, wysiwyg, layout, breakpoints, zindex без удвоенного сегмента). Значения за именами те же — сверено по 733 парам, визуально ничего не меняется.
Потребителю: Разметка и классы прежние, править нечего; блокам придёт обновлённая секция CSS. Публикация блоков — только вместе с публикацией темы: старая тема на проде новых имён не знает.

## 1.1.1 — 2026-08-30 — PATCH
Описание скилла сокращено до имени и базового класса: маршрут к компоненту переехал в awds-components.
Потребителю: Ничего делать не нужно — разметка, CSS и токены не менялись.

## 1.1.0 — 2026-08-28 — MINOR
Размер цены переехал из price в карточку: правило .pcard .price переопределяет аккумуляторы --awds-price-fs/-lh/-ls на роли WYSIWYG price-listing / caption, плейсхолдер — на body. Раньше это делал класс .price--listing внутри price; он убран в price 2.0.0, потому что размер, зависящий от типографики контекста, не может быть ступенью шкалы Control, которая от контекста не зависит. Карточка уже задаёт вид вложенным компонентам (.btn-favorites, .slider__dot) — цена встала туда же.
Потребителю: Вид не меняется: значения те же токены. Но правки парные — секции product-card и price пересобирать вместе (sync-component-css), иначе цена в карточке упадёт на дефолтный размер 600. В разметке убрать price--listing у .price внутри .pcard.

## 1.0.5 — 2026-08-26 — PATCH · visual
Цвета ссылок, звезды рейтинга и степпера переведены на ячейки State: link/accent, link/muted, rating/selected/bg, form-control/secondary — по привязкам из макета. Hover рубрики теперь задаёт ячейка link/muted (surface-on-highest вместо акцентного).
Потребителю: Пересобрать секцию компонента в блоках. Hover рубрики в варианте buy-now меняет цвет: был акцентный красный, стал тёмный — так задаёт ячейка студии.

## 1.0.4 — 2026-08-26 — PATCH
Цвета состояний переведены с инлайна ролей на ячейки слоя State темы студии.
Потребителю: Ничего: конечные значения совпали до одного (проверено резолвом 858 замен). Пересобрать копию CSS в блоках.

## 1.0.3 — 2026-08-25 — PATCH
Превью ссылается на derived.css: слой производных переименован из studio-compat.css (в нём остался только композит тени).
Потребителю: Ничего: правка только в preview.html, CSS компонента не менялся.

## 1.0.2 — 2026-08-25 — PATCH
Роли типографики: ссылки переведены с compat-слоя --awds-type-* на студийные --awds-wysiwyg-*.
Потребителю: пересобрать копию CSS. Вид не меняется.

## 1.0.1 — 2026-08-25 — PATCH
Ширина карточки переведена с числа 368px на токен раскладки --awds-layout-layout-market-card-slider-max: у токена есть адаптив, число его теряло. Копии в блоках ссылались на токен — компонент отставал от них.
Потребителю: пересобрать копию CSS. Вид на десктопе не меняется (токен = 368px), но карточка снова следует адаптиву раскладки на планшете и мобиле.

## 1.0.0 — 2026-08-24 — INIT · visual
Точка отсчёта версий. В состав входит переезд кольца фокуса в слой awds-component-focus-selection (21.08): у потребителей стоит прежняя геометрия, поэтому релиз помечен visual.
Потребителю: пересобрать копии CSS в блоках и посмотреть превью — зазор кольца фокуса изменился.
