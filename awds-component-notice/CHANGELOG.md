# notice — журнал версий

## 2.0.4 — 2026-09-26 — PATCH
Толщина обводки перестала быть литералом: 1px заменён на var(--awds-space-px) — цель ячейки size/outline/offset. Значение то же (1px), меняется источник: толщина правится в студии, а не в файле компонента. Обоснование «ячейки толщины в студии нет» в own_values протухло — ячейка заведена, запись снята.
Потребителю: Пересобрать копию CSS: вид не меняется, 1px остаётся 1px. Разметка, классы и контракт прежние.

## 2.0.3 — 2026-09-15 — PATCH · visual
Якоря #cell обёрнуты в комментарий /* */. Голый якорь, введённый 15.09.2026, комментарием не является: парсер CSS считает его битым объявлением и отбрасывает вместе со всем до следующей точки с запятой, то есть съедает следующую строку. Восстановлены chroma, border и color: 22 элемента превью из 76.
Потребителю: Пересобрать копию CSS: блокам уедет исправленная секция.

## 2.0.2 — 2026-09-15 — PATCH
Проставлены якоря #cell: каждая строка, читающая роль, называет ячейку макета, из которой роль взята. Меняются только комментарии — значения, классы и контракт те же. Нужно для гейта component-cell-drift.mjs, который сверяет цель ячейки с ролью в коде.
Потребителю: Ничего делать не нужно: изменились только комментарии в CSS.

## 2.0.1 — 2026-09-14 — PATCH · visual
Код переведён с ячеек State/Size на их цели: цвета трёх вариантов читают роли через ячейки badge/system, размеры — шаги space и control вместо --awds-size-notice-*. Макет перечитан из нового файла 💠 arrow ↪ components (секция 5:31). В макете видимая пилюля приведена к токену notice/{N}/size: раньше токен держал ВНЕШНЮЮ рамку, а сама пилюля была на 2-4px меньше (600 → 24 при size 28, 400 и 300 → 18 при 20). Решение владельца: прав код, лишний зазор убран. Ссылки в трёх reference-файлах переведены на новый файл; в published исправлен ключ primary, который вёл на несуществующий notice-primary.md.
Потребителю: ВИД МЕНЯЕТСЯ, и это починка. До правки CSS ссылался на имена, которых в опубликованной теме нет (State и Size приватны с 03.09.2026): плашка выходила без отступов, с цветом по наследству. Замер превью: 40 элементов. Теперь рисуется как задумано. Геометрия макета к коду не меняется — код и был прав.

## 2.0.0 — 2026-09-13 — MAJOR
Вариант primary переименован в accent: имя варианта теперь совпадает с группой ячеек темы badge/system/accent, которую он и читает. Мета и снапшот переведены на файл 💠 arrow ↪ components.
Потребителю: Заменить класс .notice-primary на .notice-accent — CSS под старым именем больше нет.

## 1.0.5 — 2026-09-02 — PATCH
Имена переменных темы переведены на новые после переименования семейств в студии 02.09.2026 (space, opacity, typography, wysiwyg, layout, breakpoints, zindex без удвоенного сегмента). Значения за именами те же — сверено по 733 парам, визуально ничего не меняется.
Потребителю: Разметка и классы прежние, править нечего; блокам придёт обновлённая секция CSS. Публикация блоков — только вместе с публикацией темы: старая тема на проде новых имён не знает.

## 1.0.4 — 2026-08-30 — PATCH
Описание скилла сокращено до имени и базового класса: маршрут к компоненту переехал в awds-components.
Потребителю: Ничего делать не нужно — разметка, CSS и токены не менялись.

## 1.0.3 — 2026-08-26 — PATCH
Цвета вариантов переведены на ячейки State набора badge/system (accent, secondary, ghost) — та же привязка, что в макете ноды 2093:552. Прозрачность ghost теперь даёт ячейка (extended-transparent), а не литерал transparent.
Потребителю: Пересобрать секцию компонента в блоках. Вид не меняется: все 12 значений совпали до одного.

## 1.0.2 — 2026-08-26 — PATCH
Превью ссылается на derived.css: слой производных переименован из studio-compat.css.
Потребителю: Ничего: правка только в preview.html, CSS компонента не менялся.

## 1.0.1 — 2026-08-25 — PATCH
Превью ссылается на derived.css: слой производных переименован из studio-compat.css (в нём остался только композит тени).
Потребителю: Ничего: правка только в preview.html, CSS компонента не менялся.

## 1.0.0 — 2026-08-24 — INIT
Точка отсчёта версий. Артефакты зафиксированы как есть, вид не менялся.
Потребителю: ничего.
