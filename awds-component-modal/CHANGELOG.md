# modal — журнал версий

## 1.0.1 — 2026-09-23 — PATCH
Ссылка на переименованный токен лейаута: --awds-layout-section-side → --awds-layout-section-gutter. Значение прежнее (16 → 16 → 24), вид не изменился.
Потребителю: После публикации темы обновить копию CSS в блоках через sync-component-css.mjs: переменной со старым именем в теме больше нет.

## 1.0.0 — 2026-09-22 — стартовая версия
Компонент заведён из макета: окно поверх страницы на нативном <dialog> — шапка с заголовком, прокручиваемое содержимое, подвал с кнопками; два вида, диалог по центру и панель у края, на мобильном оба становятся шторкой снизу. Макет приведён к канону: вид стал именем набора, ось view признана брейкпоинтом, части разобраны с 23 ячеек до 5, 41 привязка переведена со старых библиотек.
Потребителю: Новый компонент, ломать нечего. Ловушку фокуса и Esc даёт showModal(), своего JS для этого писать не нужно.
