# button — журнал версий

## 2.2.7 — 2026-09-26 — PATCH
Толщина обводки перестала быть литералом: 1px заменён на var(--awds-space-px) — цель ячейки size/outline/offset. Значение то же (1px), меняется источник: толщина правится в студии, а не в файле компонента. Обоснование «ячейки толщины в студии нет» в own_values протухло — ячейка заведена, запись снята.
Потребителю: Пересобрать копию CSS: вид не меняется, 1px остаётся 1px. Разметка, классы и контракт прежние.

## 2.2.6 — 2026-09-25 — PATCH · visual
Фон вариантов clean и pills стал плоской заливкой — та же правка, что у secondary (2.2.4) и tertiary (2.2.5): градиент chroma→bg базы переопределён правилами .btn.btn-clean и .btn.btn-pills, ячейки chroma-* этих вариантов компонентом больше не читаются. Макеты 8:7893 и 8:8412 переведены на сплошную заливку, по 84 варианта.
Потребителю: Разметка и классы прежние. У clean вид не меняется — там градиент и раньше был невидим (chroma совпадал с bg). У pills при наведении вместо перехода f5f5f5→e2e2e2 сплошной e2e2e2. Потребителям с копией CSS нужна пересборка секции.

## 2.2.5 — 2026-09-24 — PATCH · visual
Фон варианта tertiary стал плоской заливкой: градиент chroma→bg базы переопределён правилом .btn.btn-tertiary { background: var(--awds-btn-bg) }, ячейки button/tertiary/chroma-* компонентом больше не читаются. Макет 8:6855 переведён на сплошную заливку у всех 84 вариантов — та же правка, что у secondary в 2.2.4.
Потребителю: Разметка и классы прежние. Меняется вид tertiary при наведении: вместо перехода eff1fb→d2d7f2 сплошной d2d7f2. Потребителям с копией CSS нужна пересборка секции.

## 2.2.4 — 2026-09-24 — PATCH · visual
Фон варианта secondary стал плоской заливкой: градиент chroma→bg базы переопределён правилом .btn.btn-secondary { background: var(--awds-btn-bg) }, ячейки button/secondary/chroma-* компонентом больше не читаются. Макет 8:5298 переведён на сплошную заливку у всех 84 вариантов. Заодно pills/color-hover подтянут за студией на accent/core.
Потребителю: Разметка и классы прежние. Меняется вид secondary при наведении: вместо перехода f5f5f5→e2e2e2 сплошной e2e2e2; у pills меняется цвет подписи в ховере. Потребителям с копией CSS нужна пересборка секции.

## 2.2.3 — 2026-09-15 — PATCH
Вариант ghost переведён с ключевого слова transparent на роль extended/transparent и ячейки button/ghost/{bg,chroma,border}: 12 строк, 12 якорей. Ячейки заведены в студии заново по решению владельца — их снесли в Э1 как «инвариантные без читателей», но читатель есть, и макет всё это время держал 168 обводок и 168 стопов градиента именно на этой роли. Вид не меняется: роль равна 255 255 255 / 0%, замер превью показал только ложный дифф спиннера (rect в разных фазах вращения).
Потребителю: Ничего делать не нужно: вид тот же. Блокам уедет обновлённая секция.

## 2.2.2 — 2026-09-15 — PATCH · visual
Код переведён с имён ячеек слоёв State и Size на их цели — роли и базовые шкалы: 452 ссылки в восьми файлах вариантов, у каждой строки якорь #cell с адресом ячейки. Это починка, а не косметика: имён --awds-size-* и --awds-state-* в опубликованной теме нет с 03.09.2026, объявления молча отбрасывались. Замер превью до и после — 131 изменившийся элемент из 260: кнопка рисовалась без отступов, без скругления, без обводки, шрифтом 14 вместо 16 и высотой 20px вместо 52px. Заодно uses приведён к фактическим версиям: focus-selection 2.0.9, progress 2.0.2.
Потребителю: Пересобрать копию CSS — блокам уедет исправленная секция. Разметка, классы и контракт не менялись; кнопка начнёт рисоваться так, как нарисована в макете.

## 2.2.1 — 2026-09-14 — PATCH
Разметка в сниппетах и комментарии переведены на .progress--indeterminate: progress 2.0.0 убрал модификатор с класса варианта (правило 11).
Потребителю: Ничего делать не нужно, если разметка берётся из сниппетов скилла. Свой скопированный ранее код с progress-circular--indeterminate поправить: класс больше не объявлен.

## 2.2.0 — 2026-09-14 — MINOR
Загрузка стала шестым значением оси state в макете: ось loading убрана, 1120 ячеек секции сжались до 672 (140 → 84 на тон). Способ выбран потому, что BOOLEAN в Figma управляет только видимостью слоя, а контент под спиннером гасится opacity — одним булевым это не выражается; тот же приём уже применён у progress. В коде механика не изменилась, .btn--loading остался классом. Восемь безсрезовых ячеек темы доведены до полного набора -rest/-hover/-focus/-active (874 → 898), и каждое состояние в CSS теперь читает свой срез вместо rest.
Потребителю: Ничего делать не нужно: классы и значения те же. Состояние loading теперь заявлено в контракте — класс .btn--loading существовал и раньше.

## 2.1.0 — 2026-09-14 — MINOR
Добавлен модификатор формы .btn--pill — независимая ось, сочетается с любым тоном и размером; скругление из --awds-rounded-border-radius-full. В макете набор button / pills переведён под мод скругления (все ступени радиуса 26px), в коде форма вынесена отдельным классом, а не вшита в тон: pills отличается от secondary только акцентным текстом на hover. Слоты иконок в частях . / prefix, . / suffix, . / center переименованы ic20-a/b/color → ic-a/b/color (63 слоя) — канон правила 12, пиксель в имени слота врал на ступенях 200…50, где иконка 16px.
Потребителю: Ничего менять не нужно: класс необязательный. Канонический вид варианта pills теперь btn btn-pills btn--pill — без модификатора он останется прямоугольным, как и был.

## 2.0.6 — 2026-09-14 — PATCH
Переведён на новый файл Figma «💠 arrow ↪ components», секция ↪ button (5:7): снапшот, шапки восьми CSS, ссылки в восьми reference-файлах. Оси приведены к канону — Type→content[text|icon], Progress→loading[off|on], значения в нижнем регистре; восемь тонов теперь наборы одной секции, 140 ячеек на тон. Сплошной аудит цвета: 10536 узлов, 6400 привязанных заливок, сырых НОЛЬ. Описание focus в семи CSS уточнено — тело давно берёт собственные ячейки bg/chroma/border-focus, а не цвета rest. Зависимости догнаны до фактических версий: progress 1.1.3→1.3.0, focus-selection 2.0.3→2.0.6. Код не менялся.
Потребителю: Ничего делать не нужно: классы, переменные и значения те же.

## 2.0.5 — 2026-09-02 — PATCH
Инвариантные ячейки слоя State схлопнуты в одну без суффикса состояния (решение владельца 02.09.2026, план 2026-09-02 - state-vocab-decisions.md): компонент читает --awds-state-…-bg вместо четвёрки -rest/-hover/-focus/-active с одинаковыми значениями. Значения не менялись.
Потребителю: Разметка и классы прежние; блокам придёт обновлённая секция. Публикация блоков и темы — после Э3.

## 2.0.4 — 2026-09-02 — PATCH
Имена переменных темы переведены на новые после переименования семейств в студии 02.09.2026 (space, opacity, typography, wysiwyg, layout, breakpoints, zindex без удвоенного сегмента). Значения за именами те же — сверено по 733 парам, визуально ничего не меняется.
Потребителю: Разметка и классы прежние, править нечего; блокам придёт обновлённая секция CSS. Публикация блоков — только вместе с публикацией темы: старая тема на проде новых имён не знает.

## 2.0.3 — 2026-09-01 — PATCH
Ссылки на слой State переведены с mode-less имён на плоские суффиксные: opacity/control-disabled, opacity/control-enabled → те же ячейки с суффиксом -rest. Значение не изменилось: у этих ячеек все четыре состояния (rest, hover, focus, active) несут одно и то же, поэтому -rest подставлен без потери смысла. Смысл правки — снять зависимость компонента от блоков-обёрток темы, которые переключают mode-less имена за него и новых компонентов уже не покрывают. Этап Э2 плана «Уплощение слоя State».
Потребителю: Ничего делать не надо: имена читаются внутри CSS компонента, разметка, классы и вид не менялись.

## 2.0.2 — 2026-09-01 — PATCH
Гашение переведено с литерала --awds-opacity-opacity-40 на ячейки слоя State: выключенное состояние читает --awds-state-opacity-control-disabled, включённое — парную --awds-state-opacity-control-enabled. Значение за ячейкой то же, что было за литералом (40 % и 100 %). Скилл и рецепты приведены к тому же имени ячейки.
Потребителю: Ничего делать не надо: вид не изменился. Если у вас лежит своя копия CSS — пересоберите её, чтобы гашение поехало из темы, а не из литерала.

## 2.0.1 — 2026-08-30 — PATCH
Описание скилла сокращено до имени и базового класса: маршрут к компоненту переехал в awds-components.
Потребителю: Ничего делать не нужно — разметка, CSS и токены не менялись.

## 2.0.0 — 2026-08-29 — MAJOR · visual
Кнопка больше не рисует свой спиннер: .btn__progress стал слотом, внутри него кольцо awds-component-progress. Убраны @keyframes btn-spin и сырые числа дуги из рецептов; reduced-motion теперь решает progress
Потребителю: Править разметку загрузки: на <svg class="btn__progress"> добавить progress progress-circular progress-circular--indeterminate и заменить содержимое на <circle class="progress-circular__arc" cx="12" cy="12" r="10" pathLength="100"/>. Старая разметка не сломает вёрстку, но дуга перестанет крутиться. Блоку нужна секция progress рядом с секцией button

## 1.0.5 — 2026-08-26 — PATCH
Цвета состояний переведены с инлайна ролей на ячейки слоя State темы студии.
Потребителю: Ничего: конечные значения совпали до одного (проверено резолвом 858 замен). Пересобрать копию CSS в блоках.

## 1.0.4 — 2026-08-26 — PATCH
Цвета состояний переведены с инлайна ролей на ячейки слоя State (116 объявлений); правило кольца убрано — его рисует слой focus-selection.
Потребителю: Ничего: computed совпал 116 из 116, кольцо 2px/1px не изменилось. Требуется секция слоя focus-selection в блоке (её ставит sync-component-css).

## 1.0.3 — 2026-08-25 — PATCH
Ссылки на справочники ADS переведены на tokens-map.md: шесть справочников со значениями удалены, значения живут в studio-vars.css.
Потребителю: Ничего: правка только в документации компонента, CSS не менялся.

## 1.0.2 — 2026-08-25 — PATCH
Превью ссылается на derived.css: слой производных переименован из studio-compat.css (в нём остался только композит тени).
Потребителю: Ничего: правка только в preview.html, CSS компонента не менялся.

## 1.0.1 — 2026-08-25 — PATCH
В компонент подняты две вещи, которые жили только в копиях внутри блоков: подавление вспышки тапа (-webkit-tap-highlight-color) и cursor: default у выключенной кнопки.
Потребителю: пересобрать копию CSS (sync-component-css). Вид не меняется; на мобильных пропадает серая вспышка при тапе, у disabled-кнопки курсор перестаёт обещать клик.

## 1.0.0 — 2026-08-24 — INIT · visual
Точка отсчёта версий. В состав входит переезд кольца фокуса в слой awds-component-focus-selection (21.08): у потребителей стоит прежняя геометрия, поэтому релиз помечен visual.
Потребителю: пересобрать копии CSS в блоках и посмотреть превью — зазор кольца фокуса изменился.
