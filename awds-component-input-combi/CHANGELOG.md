# input-combi — журнал версий

## 1.0.14 — 2026-09-25 — PATCH · visual
Ячейка form-control/{default,success,error,autofill}/bg-focus переехала в студии: --surface-bright → --primary-container-base. Код и документация переведены на новую цель.
Потребителю: Разметка и классы прежние. Подложка контрола при фокусе стала брендовой вместо белой. Потребителям с копией CSS нужна пересборка секции.

## 1.0.13 — 2026-09-25 — PATCH · visual
Фон стал плоской заливкой: из базы убран linear-gradient(chroma → bg), ячейки chroma-* компонентом больше не читаются, сняты регистрация @property и строка transition по ним. Градиент был виден в состоянии фокуса.
Потребителю: Разметка и классы прежние. Фон контрола теперь ровный — заметно там, где градиент был виден (в состоянии фокуса). Потребителям с копией CSS нужна пересборка секции.

## 1.0.12 — 2026-09-16 — PATCH · visual
Починка: 385 ссылок на приватные ячейки State и Size переведены на роли и базовые шкалы с якорями, вертикали комби-раскладки — на ветку rectangle/{N}/combi. До правки браузер выбрасывал объявления с несуществующими именами: поле шло без фона, рамки, скругления и всех вертикалей метки и значения (328 из 393 элементов превью). Толщина рамки взята из --awds-space-px, own_value «1px» закрыт.
Потребителю: Разметка и классы не менялись. Вид возвращается к макетному.

## 1.0.11 — 2026-09-14 — PATCH
Комментарии и описания в CSS приведены к новому имени ячейки темы: form-control/*/placehold и list/*/placehold стали placeholder после исправления опечатки по всей цепочке 14.09.2026. Значения и селекторы не менялись.
Потребителю: Ничего делать не нужно.

## 1.0.10 — 2026-09-14 — PATCH
Опечатка placehold исправлена по всей цепочке: 45 ячеек темы в студии переименованы в placeholder, обновлены снимок темы, карта токенов и tokens-map, ссылки в CSS переведены на --awds-state-form-control-*-placeholder-*, приватный аккумулятор компонента переименован следом. Значения не менялись — сверка студии показала 0 расхождений.
Потребителю: Ничего делать не нужно, если блок не переопределяет приватную переменную компонента. Если переопределяет — имя сменилось с -placehold на -placeholder; рассылка CSS обновит блоки сама.

## 1.0.9 — 2026-09-02 — PATCH
Инвариантные ячейки слоя State схлопнуты в одну без суффикса состояния (решение владельца 02.09.2026, план 2026-09-02 - state-vocab-decisions.md): компонент читает --awds-state-…-bg вместо четвёрки -rest/-hover/-focus/-active с одинаковыми значениями. Значения не менялись.
Потребителю: Разметка и классы прежние; блокам придёт обновлённая секция. Публикация блоков и темы — после Э3.

## 1.0.8 — 2026-09-02 — PATCH
Имена переменных темы переведены на новые после переименования семейств в студии 02.09.2026 (space, opacity, typography, wysiwyg, layout, breakpoints, zindex без удвоенного сегмента). Значения за именами те же — сверено по 733 парам, визуально ничего не меняется.
Потребителю: Разметка и классы прежние, править нечего; блокам придёт обновлённая секция CSS. Публикация блоков — только вместе с публикацией темы: старая тема на проде новых имён не знает.

## 1.0.7 — 2026-09-01 — PATCH
Ссылки на слой State переведены с mode-less имён на плоские суффиксные: opacity/control-disabled, opacity/control-enabled → те же ячейки с суффиксом -rest. Значение не изменилось: у этих ячеек все четыре состояния (rest, hover, focus, active) несут одно и то же, поэтому -rest подставлен без потери смысла. Смысл правки — снять зависимость компонента от блоков-обёрток темы, которые переключают mode-less имена за него и новых компонентов уже не покрывают. Этап Э2 плана «Уплощение слоя State».
Потребителю: Ничего делать не надо: имена читаются внутри CSS компонента, разметка, классы и вид не менялись.

## 1.0.6 — 2026-09-01 — PATCH
Гашение переведено с литерала --awds-opacity-opacity-40 на ячейки слоя State: выключенное состояние читает --awds-state-opacity-control-disabled, включённое — парную --awds-state-opacity-control-enabled. Значение за ячейкой то же, что было за литералом (40 % и 100 %). Скилл и рецепты приведены к тому же имени ячейки.
Потребителю: Ничего делать не надо: вид не изменился. Если у вас лежит своя копия CSS — пересоберите её, чтобы гашение поехало из темы, а не из литерала.

## 1.0.5 — 2026-08-30 — PATCH
Описание скилла сокращено до имени и базового класса: маршрут к компоненту переехал в awds-components.
Потребителю: Ничего делать не нужно — разметка, CSS и токены не менялись.

## 1.0.4 — 2026-08-28 — PATCH
Слой фокуса переименовал переменные тона: --awds-focus-color-formcontrol → -muted, --awds-focus-color-tab → -accent (focus-selection 2.0.0). Ссылки в CSS компонента обновлены.
Потребителю: Пересобрать копию CSS: значения не менялись, поменялись имена переменных.

## 1.0.3 — 2026-08-26 — PATCH
Дозаведены ячейки State: состояние при нажатии без фокуса, рамка полей, варианты accent и heading у области нажатия (на ячейки link).
Потребителю: Ничего: конечные значения совпали (проверено резолвом 887 замен). Пересобрать копию CSS в блоках.

## 1.0.2 — 2026-08-26 — PATCH
Цвета состояний переведены с инлайна ролей на ячейки слоя State темы студии.
Потребителю: Ничего: конечные значения совпали до одного (проверено резолвом 858 замен). Пересобрать копию CSS в блоках.

## 1.0.1 — 2026-08-25 — PATCH
Превью ссылается на derived.css: слой производных переименован из studio-compat.css (в нём остался только композит тени).
Потребителю: Ничего: правка только в preview.html, CSS компонента не менялся.

## 1.0.0 — 2026-08-24 — INIT · visual
Точка отсчёта версий. В состав входит переезд кольца фокуса в слой awds-component-focus-selection (21.08): у потребителей стоит прежняя геометрия, поэтому релиз помечен visual.
Потребителю: пересобрать копии CSS в блоках и посмотреть превью — зазор кольца фокуса изменился.
